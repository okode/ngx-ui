version: 2.1

orbs:
  node: circleci/node@5

jobs:
  test:
    executor:
      name: node/default
      tag: lts-browsers
    steps:
      - checkout
      - node/install-packages
      - run:
          command: npm run test -- --watch=false

workflows:
  default:
    jobs:
      - test
